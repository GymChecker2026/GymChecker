# 月次カレンダー画像対応の検証メモ（2026-08-17時点で保留）

D-BOULDERING 立川・横浜は、記事本文にテキストではなく月間カレンダーの「画像」でセット予定を掲載している。
画像をLLM（vision）に渡して予定を抽出する方式を検証したが、日付の精度に課題が残ったため実装を保留した。
このファイルは再検討時の参考用の記録であり、コードには反映していない。

## 検証対象

- 立川: https://www.d-b-c.jp/tachikawa-news/ 内の記事「2026年　8月　カレンダー」
  （画像URL: https://www.d-b-c.jp/wp-content/uploads/tachikawa-.jpeg）
- 横浜: https://www.d-b-c.jp/top/yokohama/news/ 内の記事「8月営業カレンダー」
  （画像URL: https://www.d-b-c.jp/wp-content/uploads/1-9.png）
- モデル: claude-haiku-4-5-20251001（vision入力）

## カレンダー画像の特定方法（有効だった）

記事ページ内の `<img>` タグのうち、`src` が `/wp-content/uploads/` 配下のものだけを対象にする。
ロゴ・アイコン・ボタン画像は `/wp-content/themes/.../images/` や `/assets/` 配下にあり、`uploads/` には来ないため、
この2ページで確認した限りでは確実に区別できた（各ページ6〜11個の画像中、カレンダー画像だけが1件ヒット、誤検出なし）。

立川の記事は `<article class="article_body">` でカレンダー画像を囲んでいたが、横浜の記事にはこのクラスがなく、
`uploads/` パスでのフィルタのみが両店舗で共通して機能した。

## 抽出プロンプトの調整点

既存のテキスト抽出用プロンプト（`extract_events.py` の `PROMPT_TEMPLATE`）をベースに、以下を追加/調整した。
- 種別5分類・除外リストは同じものを流用
- 「毎週末開催などの定例スクール案内を含む」を除外ルールに明記 → 「キッズスクール体験会」（毎週土日に記載）は安定して正しく除外された
- 「祝日名のみの記載は除外」を追加 → 効果は不安定（後述）

## 見つかった精度課題

### 1. 祝日名の誤検出（不安定）
「山の日」（祝日名のみで営業時間変更の記載なし）を「時間変更」イベントとして誤抽出することがある。
除外ルールをプロンプトに明記しても、実行のたびに出たり出なかったりし、LLMの指示追従だけでは信頼できない。

### 2. 複数日にまたがるように見える枠の日付ズレ（未解決）
カレンダー内の罫線で囲まれた予定枠（例:「ボルダーセット:B壁」）について、モデルは当初一貫して実際より1日後ろにずれた日付を出力した（実測上の正解は8/12〜13の位置なのに8/13〜14と回答、4回中4回で再現）。

**ピクセル解析による原因究明**（グリッド線とボックスの境界をPillowで検出し、日付列との重なりを計測）：
立川8月画像で3つの予定枠（A壁=4-5日想定、B壁=12-13日想定、C壁=26-27日想定）を実測したところ、
**3枠とも同一パターン**を示した。1つの日付列を100%覆い、両隣の列に41〜71%はみ出す、という形。

| 枠 | 完全に覆う列 | 左隣への重なり | 右隣への重なり |
|---|---|---|---|
| A壁 | 4日（Tue, 100%） | 3日に71% | 5日に41% |
| B壁 | 13日（Thu, 100%） | 12日に58% | 14日に55% |
| C壁 | 27日（Thu, 100%） | 26日に65% | 28日に48% |

これは「2日間に均等にまたがる箱」ではなく、**テキストを収めるために横長に描かれた「1日だけの予定」**である可能性が高いことを示唆する。つまり日付のズレは、モデルの列カウント能力の問題というより、そもそも「2日間の予定」という前提自体が誤っている可能性がある。

### 3. 上記知見をプロンプトに反映した再検証（一部改善、未完全）
「まず日付セルの番号を1つずつ確認してから対応させる」「はみ出しが左右均等でなければ単日として扱う」という指示を追加して再実行（3回）：
- B壁: 単日（8/13）として正しく判定されるようになった（実測と一致）
- C壁: 単日と判定するようになったが、日付自体が1日ズレたまま（8/26、実測上の正解は8/27）
- A壁: 相変わらず範囲（8/4〜5）のまま
- 「山の日」の誤検出は引き続き発生

つまりプロンプト指示だけでは安定しない。人間による目視確認や、画像処理で機械的に日付を確定する仕組み（今回の検証で使ったグリッド線・ボックス検出をコード化し、LLMには文字読み取りだけを任せる案）が必要になりそう。

### 4. その他
- OCR誤読：「オートビレイ壁」を「オートドレイ壁」（ビ→ド）と一貫して誤読。
- 画像を2倍解像度にアップスケールして再試行したところ、精度はむしろ悪化した（除外すべきキッズスクール体験会が再混入、B壁の日付ズレも拡大）。等倍の方が安定していた。

## 結論・保留の理由

プロンプト調整だけでは実用に足る精度に到達しなかった。日付枠の位置を確実に読み取るには、LLMのvision判断に頼らず、
画像処理（グリッド線・ボックス検出）で座標から機械的に日付を計算する実装が必要と考えられるが、
店舗・月ごとにレイアウトが変わりうる点を含めて実装コストが見合うか要検討のため、いったん保留とした。

再開する場合の候補案：
- グリッド線・罫線ボックスの座標検出をコード化し、日付の確定はピクセル計算で行う。LLM（またはOCR）は枠内テキストの読み取りにのみ使う。
- 差分検知は画像URLの変化で行う想定（アイデアのみ、未実装）。
